# {{ api_spec.name }} MCP Server

{{ api_spec.description or "MCP server for " ~ api_spec.name }}

**Auto-generated by [MCP Universal Adapter](https://github.com/gacabartosz/mcp-universal-adapter)**

---

## Overview

- **API Version:** {{ api_spec.version }}
- **Base URL:** {{ api_spec.primary_server }}
- **Total Tools:** {{ total_endpoints }}
{% if has_auth %}
- **Authentication:** {{ auth_type.value }}
{% endif %}

---

## Installation

```bash
# Install dependencies
pip install -e .

# Or using uv (recommended)
uv pip install -e .
```

---

## Configuration

{% if has_auth %}
### Authentication

This API requires authentication. Create a `.env` file:

```bash
cp .env.example .env
```

Then edit `.env` and add your credentials:

{% if auth_type.value == "api_key" %}
```bash
API_KEY=your_api_key_here
```
{% elif auth_type.value == "bearer" %}
```bash
BEARER_TOKEN=your_bearer_token_here
```
{% elif auth_type.value == "basic" %}
```bash
API_USERNAME=your_username
API_PASSWORD=your_password
```
{% endif %}

{% else %}
This API does not require authentication.
{% endif %}

### Environment Variables

- `API_BASE_URL`: Override default base URL (optional)
{% if has_auth %}
{% if auth_type.value == "api_key" %}
- `API_KEY`: Your API key (required)
{% elif auth_type.value == "bearer" %}
- `BEARER_TOKEN`: Your bearer token (required)
{% elif auth_type.value == "basic" %}
- `API_USERNAME`: Your username (required)
- `API_PASSWORD`: Your password (required)
{% endif %}
{% endif %}

---

## Usage

### Run MCP Server

```bash
python server.py
```

### Use in Claude Desktop

Add to your Claude Desktop config (`~/Library/Application Support/Claude/claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "{{ api_spec.name|lower|replace(' ', '-') }}": {
      "command": "python",
      "args": ["/path/to/server.py"],
      "env": {
        "API_BASE_URL": "{{ api_spec.primary_server }}"{% if has_auth %},
        {% if auth_type.value == "api_key" %}
        "API_KEY": "your_api_key_here"
        {% elif auth_type.value == "bearer" %}
        "BEARER_TOKEN": "your_bearer_token_here"
        {% elif auth_type.value == "basic" %}
        "API_USERNAME": "your_username",
        "API_PASSWORD": "your_password"
        {% endif %}
        {% endif %}
      }
    }
  }
}
```

### Use in Cursor/VS Code

Add to `.cursor/mcp.json` or `.vscode/mcp.json`:

```json
{
  "{{ api_spec.name|lower|replace(' ', '-') }}": {
    "command": "python",
    "args": ["/path/to/server.py"]
  }
}
```

---

## Available Tools

{% for tag, tag_endpoints in endpoints_by_tag.items() %}
### {{ tag }}

{% for endpoint in tag_endpoints %}
#### `{{ endpoint.tool_name }}`

{{ endpoint.summary or endpoint.description or "No description" }}

**Endpoint:** `{{ endpoint.method.value }} {{ endpoint.path }}`

{% if endpoint.parameters or endpoint.request_body %}
**Parameters:**
{% for param in endpoint.parameters %}
- `{{ param.name }}` ({{ param.type }}){% if param.required %} **required**{% endif %} - {{ param.description or 'No description' }}
{% endfor %}
{% if endpoint.request_body %}
{% for prop_name, prop in endpoint.request_body.properties.items() %}
- `{{ prop_name }}` ({{ prop.type }}){% if prop_name in endpoint.request_body.required %} **required**{% endif %} - {{ prop.description or 'No description' }}
{% endfor %}
{% endif %}
{% endif %}

---

{% endfor %}
{% endfor %}

## Development

### Testing

```bash
# Install dev dependencies
pip install -e ".[dev]"

# Run tests
pytest
```

### Generated Files

- `server.py` - Main MCP server implementation
- `pyproject.toml` - Python package configuration
- `README.md` - This file
- `.env.example` - Environment variables template

---

## About MCP

Model Context Protocol (MCP) enables AI assistants to safely interact with external tools and data sources.

Learn more:
- [MCP Documentation](https://modelcontextprotocol.io)
- [MCP Universal Adapter](https://github.com/gacabartosz/mcp-universal-adapter)

---

**Generated on:** {{ "now"|default("2024") }}
**Generator:** MCP Universal Adapter
